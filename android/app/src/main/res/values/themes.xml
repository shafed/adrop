<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; Compose renders everything, so this is minimal. -->
    <style name="Theme.Adrop" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
